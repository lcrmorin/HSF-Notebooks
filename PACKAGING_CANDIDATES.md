# Packaging candidates

Functions defined inline in a notebook — rather than living in the shared
`quanttoolbox` package — that look clean and generic enough to be worth
promoting into the toolbox later, once a second notebook actually needs
the same thing. Tracked here instead of promoted immediately, so notebook
work isn't gated on a toolbox release for every reusable-looking helper.

Each row also checks whether the function is really filling a gap, or
whether `numpy`/`scipy`/`pandas`/`scikit-learn` already do the job —
promoting a thin reimplementation of something the standard scientific
Python stack already ships isn't worth it; calling the library directly
is. "None" means no real equivalent exists; "Partial"/"Close" means a
library covers part of it or covers it under a different convention;
"Yes" means an existing library call could likely replace the function
outright.

Status legend: 🔵 candidate (used once so far) · 🟡 confirmed reusable
(used in ≥2 notebooks) · ✅ packaged into `quanttoolbox`

| Function | Defined in | What it does | Why it's a candidate | Native-library check | Status |
|---|---|---|---|---|---|
| `expected_hitting_time(P, target_states)` | `02a_ratings_and_transitions.ipynb` | Expected number of steps to reach a target set of states from each state of a discrete-time Markov chain, via a direct linear solve. | A generic Markov-chain primitive — any notebook analyzing rating/state transitions could reuse it as-is. | None — no `scipy`/`numpy` Markov-chain hitting-time helper; the underlying `numpy.linalg.solve` call is standard, but the hitting-time setup itself isn't packaged anywhere in the standard stack. | 🔵 |
| `qscore`/`zscore`/`aggregate`/`qz_score`/`zq_score`/`bz_score` | `02b_scoring_methodology.ipynb`, `15b_lasso_regularization_path.ipynb` | A small scoring-and-aggregation framework: rank-based, standardization-based, and Beta-shaped transforms of a raw criterion onto a common scale, plus weighted aggregation of already-computed scores. | Generic score-normalization primitives; the z-score formula is reused (as `standardize`) in `15b`. | Partial — `scipy.stats.zscore` and `scipy.stats.rankdata` already cover the individual `zscore`/`qscore` transforms, and `sklearn.preprocessing.QuantileTransformer` covers a fancier version of the same idea; only the Beta-shaped transform and the weighted-aggregation layer aren't off-the-shelf. | 🟡 |
| `corr_from_lower_triangle(values, n)` | `03a_esg_risk_and_factors.ipynb`, `03b_pastor_pedersen_te.ipynb`, `11k_net_zero_core_satellite.ipynb`, `16f_chap11_carbon_portfolio_optimization.ipynb` | Builds a symmetric n x n correlation matrix from its lower-triangular entries, listed row by row. | A generic array-construction utility with no domain content — reused as-is in four notebooks so far. | Considered and passed over — `scipy.spatial.distance.squareform` needs a diagonal-free condensed vector in upper-triangular row-major order, while `values` here is lower-triangular row-major *including* the diagonal. Swapping would mean manually reordering every hardcoded correlation literal at each of the four call sites, risking a silent transcription error in the matrix itself — not worth it for a matrix this small. | 🟡 |
| `beta_pi_alpha(x, mu, Sigma, r)` | `03b_pastor_pedersen_te.ipynb` | CAPM beta/pi/alpha decomposition of every asset against a given portfolio used as the pricing benchmark. | A generic portfolio-attribution primitive, reused three times within `03b` alone. | None — this is standard finance math, but no numpy/scipy/pandas function computes a CAPM beta/pi/alpha attribution directly; `statsmodels.OLS` could recover beta via regression, but not the full three-way decomposition in one call. | 🔵 |
| `stacked_issuance_outstanding(...)` / `load_cbi_sheet(...)` | `04b_gss_bond_market.ipynb` | Reads a "year-row + category-rows" wide time-series sheet and renders the standard issuance/cumulative-outstanding two-panel chart plus an end-of-sample share table. | Reused 6 times within `04b`; the same shape is plausible for other market-sizing data later in the book. | None — `pandas.read_excel`/`pivot_table` supply the building blocks, but the specific wide-sheet layout and the two-panel chart convention are project-specific, not something a library packages. | 🔵 |
| `bond_ytm(t, cashflows, price)` | `04c_the_greenium.ipynb` | Newton's-method solver for the yield-to-maturity implied by a cashflow schedule and price. | A basic fixed-income building block, likely needed anywhere a bond is priced from first principles. | Close — `numpy_financial.irr` solves the same "rate implied by a cashflow schedule and price" problem for evenly-spaced cashflows; not in `numpy`/`scipy` core (it's a separate small package, split out of `numpy` a few years ago) and doesn't handle irregular timing the way `t` here does, so it's an adjacent alternative rather than a drop-in replacement. | 🔵 |
| `blended_finance_simulation(...)` | `05a_impact_market_instruments.ipynb` | Two-tranche (junior/senior) cash-flow waterfall: allocates cumulative losses and coupon income between tranches given a portfolio default path. | A generic structured-finance primitive, reused twice within `05a`. | None — structured-finance waterfalls aren't in the standard scientific stack; the closest public packages (e.g. `absbox`) are niche and not already a dependency here. | 🔵 |
| `fit_power_law(fit_table, area_units)` | `05b_biodiversity_extinction_theory.ipynb` | Backs out a power-law exponent/intercept for each row of a table from log-log ratios against a reference column. | A generic scaling-law fitting utility, not biodiversity-specific. | Partial — the dedicated `powerlaw` package (PyPI) fits power laws more rigorously (including tail cutoffs and model comparison), and `scipy.optimize.curve_fit` could fit the same two-parameter form directly; this function's specific "average the ratio across reference columns" shortcut isn't what either does out of the box. | 🔵 |
| `species_abundance_distribution` / `species_area_relationship` / `endemics_area_relationship` / `hurlbert_rarefaction` | `05c_biodiversity_abundance_models.ipynb` | Community-ecology primitives: abundance-frequency binning, random-placement species-area/endemics-area curves, and Hurlbert's exact rarefaction formula. | Reused repeatedly within `05c` across four datasets; the species-area power-law form is echoed again in `16c`. | None — Python's community-ecology tooling is thin (R's `vegan` package has no real Python equivalent); `scikit-bio` covers alpha/beta diversity indices but not these specific curve-fitting/rarefaction forms. | 🟡 |
| `pdf_normal_ratio(...)` / `cdf_normal_ratio(...)` | `08b_physics_feedbacks_sensitivity.ipynb` | Closed-form density/CDF of the ratio of two independent normal random variables — used here for equilibrium climate sensitivity. | A generic probability primitive with no climate content. | None — `scipy.stats` has no built-in "ratio of two normals" distribution (unlike the ratio of two standard normals, which is Cauchy and *is* covered by `scipy.stats.cauchy`; the general case with different means/variances isn't). | 🔵 |
| `ice_albedo(...)` / `ice_albedo_tau(...)` | `08c_physics_ice_albedo.ipynb` | Evaluates the bistable ice-albedo energy-balance model's equilibrium condition and its local relaxation timescale. | Tied to this notebook's specific model form — a plausible reuse only if a later notebook revisits the same bistable model. | None — this is a specific physical model, not a generic numerical primitive; no library packages it. | 🔵 |
| `carbon_budget_Reduction(t0, t, CE_t0, R, mtd)` | `09a_carbon_budget_accounting.ipynb` | Closed-form carbon budget under one of three emission-decline paths (linear, compound, exponential), cross-checked against numerical integration. | A generic emissions-pathway integration primitive, reused 3 times within `09a`. | None for the closed forms themselves — `scipy.integrate.quad` already supplies the numerical cross-check used internally, but the three closed-form antiderivatives are domain-specific and not something a library would ship. | 🔵 |
| `carbon_budget_piecewise(t0, t, t_k, CE_k)` | `09a_carbon_budget_accounting.ipynb`, `09f_transition_pathways_pac_scoring.ipynb` | Piecewise-linear multi-period carbon budget via linear interpolation between knot emission rates and trapezoidal integration. | Reused across two notebooks. | Partial — `numpy.interp` (linear interpolation) and `numpy.trapz`/`scipy.integrate.trapezoid` (trapezoidal integration) already supply both underlying steps; this function is really just gluing two one-line numpy calls together for this project's specific knot convention. | 🟡 |
| `tale_chart(ax, raw, ylim, yticks)` | `09g_cdp_netzero_tracking.ipynb` | Renders the "track record / trend / targets / scenario" comparison chart from a 5-row raw data matrix, smoothing each curve between its real data points. | A generic "actual vs. extrapolated-trend vs. target vs. scenario" chart shape, reused 3 times within `09g`. | Partial — the PCHIP smoothing step is already `scipy.interpolate.PchipInterpolator` under the hood; the chart layout/normalization logic around it is project-specific and not something matplotlib packages as a chart type. | 🔵 |
| `dwl_shift_diagram(...)` | `10d_externality_comparative_statics.ipynb` | Given a fixed linear SMB/SMC curve and a shifted version of the other, computes the comparative-statics equilibrium points and renders the labeled deadweight-loss diagram. | Reused 5 times verbatim within `10d`. | None — this is a specific textbook-diagram builder, not a numerical primitive any library would package. | 🔵 |
| `weitzman_example(...)` | `10e_weitzman_prices_vs_quantities.ipynb` | Given linear SMB/SMC curves and a discrete cost-shock distribution, computes the ex-post-optimal, price-instrument, and quantity-instrument outcomes and welfare loss. | Reused 3 times within `10e`; a clean, generic "prices vs. quantities" comparison primitive. | None — this is a specific economic model, not something numpy/scipy/statsmodels ship. | 🔵 |
| `min_te_portfolio(...)` | `11a_tracking_error_optimization_basics.ipynb`, `11h_net_zero_te_optimization.ipynb`, `16f_chap11_carbon_portfolio_optimization.ipynb` | Minimum-tracking-error QP: minimizes `(w-b)'Sigma(w-b)` subject to linear constraints and box bounds, pruning rank-deficient equality rows first. | Confirmed reusable across three notebooks now. | Partial — `scipy.optimize.minimize(method="SLSQP")` is already the underlying solver; dedicated QP packages (`cvxpy`, `qpsolvers`, `osqp`) would likely solve this specific quadratic-objective/linear-constraint problem more robustly and without needing the rank-pruning workaround below, if the project were open to adding one as a dependency. | 🟡 |
| `_prune_redundant_rows(A, b, tol)` | `11a_tracking_error_optimization_basics.ipynb`, `11h_net_zero_te_optimization.ipynb` | Incrementally drops equality-constraint rows that don't increase the matrix's rank, returning an equivalent full-row-rank system. | Fixes a real SLSQP gap — a singular Jacobian on a rank-deficient equality system otherwise silently returns the initial guess unchanged. A generic guard for any constrained QP built this way. | Close — a rank-revealing `scipy.linalg.qr` with column pivoting (or `sympy.Matrix.rref`) can identify a maximal independent row set in one call instead of the incremental `numpy.linalg.matrix_rank` loop used here; would need to confirm it produces the same row selection, not just an equally-valid one. | 🟡 |
| `quadratic_form(x, Q, R, c)` | `11c_quadratic_form_verification.ipynb`, `16f_chap11_carbon_portfolio_optimization.ipynb` | `0.5*x'Qx - x'R + c` — note the minus sign, confirmed against the chapter's own shift identity. | The chapter's most elementary building block; a wrong-sign copy elsewhere would silently produce plausible-looking but incorrect numbers. Reused again in `16f`. | N/A — this is a one-line array expression (`0.5*x@Q@x - x@R + c`), not something a library would package as a named function; the value here is the sign convention, not the arithmetic. | 🟡 |
| `quadratic_form_risk(sector, X, X_star, z)` / `_sector_risk_matrices` | `11c_quadratic_form_verification.ipynb` | Builds `(Q,R,c)` for a quadratic penalty on a vector's per-sector weighted aggregate against a target. | Reused 3+ times wherever a chapter-11 notebook builds a sector-level MD/DTS/AS penalty. | None — this is a project-specific QP-matrix builder tied to the book's sector-penalty formulation. | 🔵 |
| `quadratic_form_bond_portfolio1(...)` | `11c_quadratic_form_verification.ipynb` | Absolute-target (not benchmark-relative) mixed MD/DTS objective plus a linear carry term. | Same reuse profile as the benchmark-relative version below. | None — same reasoning as `quadratic_form_risk` above. | 🔵 |
| `quadratic_form_bond_portfolio2(...)` | `11c_quadratic_form_verification.ipynb`, `11d_decarbonization_toy_examples.ipynb`, `16f_chap11_carbon_portfolio_optimization.ipynb` | Generalizes the benchmark-relative mixed AS/MD/DTS objective to an arbitrary per-sector target plus a carry term. | Confirmed reusable across three notebooks now. | None — same reasoning as `quadratic_form_risk` above. | 🟡 |
| `bond_portfolio_metrics(sector, MD, DTS, b)` | `11c_quadratic_form_verification.ipynb` | Portfolio-level and per-sector weighted MD/DTS aggregates of a given weight vector. | A small, generic chapter-11-wide utility. | Partial — this is essentially a grouped weighted-average, which `pandas.DataFrame.groupby(...).apply(np.average, weights=...)` already computes; using pandas directly here would likely be cleaner than a bespoke function. | 🔵 |
| `solve_min_quadratic_form(...)` | `11d_decarbonization_toy_examples.ipynb` | Minimizes `quadratic_form(w,Q,R,c)` subject to linear constraints and box bounds via SLSQP. | A thin, generic bridge between a `(Q,R,c)` triple and an actual constrained solve. | Partial — same as `min_te_portfolio` above: `scipy.optimize.minimize` is already doing the work; `cvxpy`/`qpsolvers` would be the more purpose-built alternative for this exact QP shape. | 🔵 |
| `plot_four_curves(...)` | `11e_decarbonization_results.ipynb` | Renders a fixed 4-curve severity comparison with a reference line, onto a given axis. | Reused 6 times within `11e` alone. | None — plain chart-layout code; matplotlib doesn't package specific multi-curve comparison layouts. | 🟡 |
| `pathway_benchmarks()` | `11f_net_zero_pathway_benchmarks.ipynb` | Returns 4 normalized decarbonization-pathway curves (IEA, NZAOA, CTB, PAB), smoothed and rescaled to 100 at 2020. | Used by both the plot and the table in `11f`. | None — hardcoded reference-scenario data, not a numerical primitive. | 🔵 |
| `carbon_budget_compound_reduction(...)` / `estimate_delta_R(...)` | `11g_net_zero_carbon_budget.ipynb` | Cumulative-carbon-budget-under-compounding-reduction model, plus a bisection wrapper solving for the reduction rate hitting a target budget. | The version to reuse if a later notebook needs the same "reduction rate → cumulative budget" primitive. | Partial — the closed form itself has no library equivalent, but the solve-for-rate step could use `scipy.optimize.brentq` instead of the project's own `bisection`. | 🔵 |
| `solve_mixed_norm_te(...)` | `11b_l1_l2_norm_reformulation.ipynb`, `16f_chap11_carbon_portfolio_optimization.ipynb` | Solves the L1-form mixed active-share/MD/DTS bond tracking-error objective under a budget constraint, two ways (direct nonsmooth minimization and an LP reformulation). | Confirmed reused in `16f`. | Partial — `scipy.optimize.minimize`/`linprog` are already the underlying solvers for the two variants; `cvxpy` would let the L1 objective be written directly (it has native support for L1-norm terms) instead of hand-building the epigraph/slack-variable LP reformulation. | 🟡 |
| `glide_path(...)` | `11h_net_zero_te_optimization.ipynb` | A compounding-reduction closed form returned as a sequence of glide-path targets for a loop of re-optimizations. | Every net-zero glide-path re-optimization in `11h` builds this same sequence first. | None — a one-line closed-form sequence, not something a library packages. | 🔵 |
| `solve_bond_l1_duration_neutral(...)` | `11h_net_zero_te_optimization.ipynb` | Generalized LP builder for the L1 active-share+DTS bond objective subject to budget, duration-neutrality, a reduction target, and optional extra constraint rows. Returns `None` on infeasibility rather than raising. | Confirmed reusable within `11h` across two scenarios; the `None`-on-infeasible convention deliberately surfaces genuine infeasibility rather than hiding it. | Partial — `scipy.optimize.linprog` is already the underlying solver (its own status code already distinguishes infeasibility from success, so wrapping it to return `None` on that status is a thin layer); `cvxpy` would express the L1 term more directly. | 🟡 |
| `solve_bond_l1_duration_neutral_smooth(...)` | `11h_net_zero_te_optimization.ipynb` | SLSQP-based direct nonsmooth minimization of the same L1 objective, used as a cross-check against the LP solution. | Single-use so far. | Partial — `scipy.optimize.minimize` is already the underlying solver; nothing else to add beyond the note on `solve_bond_l1_duration_neutral` above. | 🔵 |
| `smoothing_spline_curve(...)` | `11i_net_zero_stock_level_results.ipynb`, `11l_net_zero_barahhou_te_decomposition.ipynb` | Fits a smoothing spline at a given smoothing parameter, plus upper-range NaN-masking. | Confirmed reused across a second notebook, covering the full smoothing range `spline_curve` above only approximates. | Close — this already wraps the third-party `csaps` package (not in `numpy`/`scipy`); `scipy.interpolate.UnivariateSpline(s=...)` is the closest thing that ships with `scipy` itself and could plausibly replace it, though its smoothing-factor convention isn't identical to `csaps`'s. | 🟡 |
| `subdivide(y_boundaries, counts)` | `11j_net_zero_taxonomy_and_thematic.ipynb` | Given parent-slot boundaries and child counts, splits each parent's slot into equal-width child slots — chained to build a full hierarchical tree's line geometry. | A generic hierarchical-bracket/treemap-boundary primitive, not specific to any one taxonomy. | None — no standard library packages this specific bracket-subdivision geometry; `matplotlib`'s own treemap-adjacent tools (e.g. via `squarify`, a separate package) solve a related but different 2-D layout problem. | 🔵 |
| `spider_plot(...)` | `11l_net_zero_barahhou_te_decomposition.ipynb` | A radar/spider chart where each axis is independently normalized to its own max value across the plotted series, on plain Cartesian geometry so it can carry per-axis scales, with an optional shaded region between two series. | The only radar chart in the book so far; worth reusing the moment a second chapter needs a per-axis-normalized radar chart. | Partial — matplotlib's `projection="polar"` gives a basic radar chart but can't do independent per-axis scaling; third-party packages (e.g. `plotly`'s `Scatterpolar`, or the PyPI `radar_chart` helpers) exist but aren't already dependencies here, so this Cartesian-geometry version still earns its keep. | 🟡 |
| `indnv(query, table)` | `11l_net_zero_barahhou_te_decomposition.ipynb` | Exact-match index lookup: for each value in `query`, returns its position in `table`, or `NaN` if not found. | A generic lookup utility, not chapter-11-specific. | Close — `pandas.Index(table).get_indexer(query)` does the same lookup, but returns `-1` for a miss rather than `NaN`; a caller relying on the `NaN` convention (e.g. for arithmetic that should propagate a missing value) would still need a small wrapper. | 🔵 |
| `wiechel_forward(lon_deg, lat_deg, R=1.0)` | `12b_geodesy_wind_and_map_projections.ipynb` | Closed-form polar-aspect Wiechel map projection. | Neither of this project's mapping libraries implement Wiechel; worth reusing directly rather than re-deriving. | None — confirmed absent from `pyproj`'s full operations list and from `Basemap`'s supported projections; a from-scratch closed form is the only option in Python for this one. | 🔵 |
| GSHHS land-polygon extraction via a cylindrical-projection basemap | `12b_geodesy_wind_and_map_projections.ipynb` | Recovers raw unprojected (lon, lat) coastline vertices offline, for use in projections not natively supported by the mapping libraries here. | A general offline-land-data pattern for any future map needing raw coastline geometry. | Partial — `cartopy`'s Natural Earth coastline data would normally cover this directly, but its download is blocked by this environment's network egress; `basemap-data`'s bundled GSHHS shapefiles are the offline workaround, not a first-choice library call. | 🔵 |
| `compute_horizontal_resolution(delta_phi, delta_lambda)` | `12c_ipcc_grid_resolution_formulas.ipynb` | Returns the IPCC-style grid-cell horizontal resolution at the equator and at 45° latitude, plus the shortcut average. | A straightforward, single-purpose utility reused wherever this chapter's grid-resolution tables come up. | None — a short domain-specific formula (not, e.g., something `pyproj`/`geopy` compute directly). | 🔵 |
| `resolution_to_angular(res)` | `12d_ipcc_assessment_report_grids.ipynb` | Converts a climate-model grid-resolution label in any of three notations (rhomboidal, triangular spectral, plain angular) into a `(delta_phi, delta_lambda)` degree pair. | Reused across 4 data sheets within this notebook already. | None — this label convention is specific to the IPCC assessment-report grid tables this notebook reads; no climate library in the standard Python stack (this project isn't using `xarray`/`xesmf`/`iris`) parses it. | 🔵 |
| `compute_stats(x)` | `12d_ipcc_assessment_report_grids.ipynb` | `[n, median, min, max, mean]` of a 1-D array, counting `n` from the original length (including NaNs) while the summary stats themselves drop NaNs first. | An easy-to-get-subtly-wrong convention, used 8 times across this notebook alone. | Close — `pandas.Series.describe()` or `scipy.stats.describe()` give a similar summary bundle in one call, but both compute their own `count` after dropping NaNs, not before — so neither reproduces this function's specific "n includes NaNs" convention without extra work. | 🟡 |
| `pdfweibull`/`cdfweibull`/`cdfweibulli`/`skewness_weibull`, `pdfgev`/`cdfgev`/`cdfgevi` | `12i_extreme_value_distributions.ipynb` | Weibull(scale, shape) and GEV(location, scale, shape) pdf/cdf/inverse-cdf/skewness, cross-checked against `scipy`'s equivalents to ~1e-17. | Provably-correct, ready-made extreme-value primitives for any later tail-risk/stress-scenario work. | Yes — `scipy.stats.weibull_min` and `scipy.stats.genextreme` already provide `.pdf`/`.cdf`/`.ppf`/`.stats` for exactly these distributions (confirmed to agree here); the only reason this wrapper exists is to present them in the parameter convention this book's formulas use, not because `scipy` is missing the distributions. If a later chapter needs these again, calling `scipy.stats` directly with a small parameter-mapping note is probably simpler than packaging a full reimplementation. | 🔵 |
| `ecb_risk_score_table(hzrd_typ)` / `ecb_loss_table(hzrd_typ)` | `12g_financial_loss_and_catastrophe_metrics.ipynb` | Filters the ECB physical-risk stress-test tables to portfolio-level rows for one hazard scenario, selecting/rescaling columns by name. | Reused twice each within `12g` already. | None beyond `pandas` itself (already used internally) — the filter/rename/rescale logic is tied to this specific dataset's column layout. | 🟡 |
| `build_climate_stressed_generator(Lambda, beta)` | `13f_markov_transition_climate_stress.ipynb` | Scales the default-column entries of a continuous-time Markov generator matrix by a stress multiplier and re-balances the diagonal so every row still sums to zero. | Fully generic — takes any generator matrix and scalar; called 3 times in this notebook. | None — no library packages this specific generator-matrix stress-and-rebalance operation. | 🔵 |
| `thresholds_from_matrix(P, Inf_limit)` | `13g_single_factor_conditional_default.ipynb` | Converts a row-stochastic transition matrix into single-factor Gaussian-copula thresholds bracketing each destination rating, capping the boundary cells. | The core "matrix → normal thresholds" step behind any Vasicek/Merton-style conditioning in this chapter. | Partial — `scipy.stats.norm.ppf` already supplies the inverse-normal step used internally; the cumulative-sum-then-threshold construction around it is project-specific. | 🔵 |
| `invcdf_default_rate(alpha, PD, rho)` | `13i_vasicek_implied_lgd_and_calibration.ipynb` | Closed-form Vasicek large-homogeneous-portfolio default-rate quantile at confidence level `alpha`. | A textbook-generic 3-argument closed form, called 24 times in a calibration grid plus repeatedly downstream. | Partial — built entirely from `scipy.stats.norm.cdf`/`.ppf`; no dedicated `scipy`/`pandas` "Vasicek default rate" function exists (credit-risk-specific packages like `creditrisk` are niche, unmaintained, and not already a dependency), so this closed-form combination is the reasonable option. | 🔵 |
| `vasicek_density(d, PD, r)` | `13j_default_rate_distribution_methods.ipynb` | Closed-form limiting density of the single-factor Vasicek portfolio default rate. | A standard credit-portfolio-risk primitive, evaluated across a correlation grid and reused in a 3-method cross-check. | Same as `invcdf_default_rate` above — built from `scipy.stats.norm`, no packaged Vasicek-density function exists in the standard stack. | 🔵 |
| `gaussian_copula_pdf_grid(u1, u2, rho)` | `15a_copula_functions_and_diagrams.ipynb` | Evaluates the bivariate Gaussian copula density over the full outer-product grid of two 1-D arrays. | A small, generic grid-evaluation adapter, worth promoting once a second notebook needs to plot a copula surface. | None beyond what's already used — it's a `numpy.meshgrid` + a call into `quanttoolbox`'s own `gaussian_copula_pdf`; no separate library needed. | 🔵 |
| `frank_level_curve(u1, t, theta)` | `15a_copula_functions_and_diagrams.ipynb` | Closed-form inversion of the Frank copula CDF to trace the level curve where the copula equals a target value. | Self-contained closed-form math; reused across a 2x2 subplot loop plus a separate validation pass. | Partial — the third-party `copulas` package (PyPI) implements several copula families but doesn't expose a direct level-curve inversion utility either; this closed form is still the practical option. | 🔵 |
| `reg_lasso2(y, x, lambda_grid, n_iters=1000)` | `15b_lasso_regularization_path.ipynb` | Loops a lasso solver over a lambda grid to build the full regularization path: coefficient matrix, L1 norm, degrees of freedom, and complexity at each lambda. | A generic path-builder around a toolbox solver, not tied to this notebook's specific dataset. | None that fits — `sklearn.linear_model.lasso_path` computes the same regularization path, but this notebook's whole point is exercising `quanttoolbox`'s own coordinate-descent `lasso_ccd` solver across a lambda grid; swapping in `scikit-learn`'s solver would replace the demonstration, not just the plumbing around it. | 🔵 |
| `rectangle_prob(mvn, xl, xu)` | `16a_chap2_esg_scoring.ipynb` | Computes a 2-D rectangle probability from any object exposing `.cdf`, via inclusion-exclusion. | Fully generic bivariate-CDF-to-rectangle-probability utility, independent of any specific distribution. | None — already correctly built on `scipy.stats.multivariate_normal.cdf`; the inclusion-exclusion combination on top is the actual value this function adds, and `scipy` has no rectangle-probability convenience method to fall back on instead. | 🔵 |
| `esg_transition_matrix(s, sigma)` | `16a_chap2_esg_scoring.ipynb` | Given score-bucket cut points and a remeasurement-noise level, builds the bucket-to-bucket transition probability matrix plus per-bucket and aggregate turnover. | Generic bucket/turnover model parameterized purely by cut points and noise level, reused across four cells. | None — a project-specific measurement-noise model; no library packages this. | 🔵 |
| `mc_weighted_score(alpha_vec, nS, n, rng, g1=None, g2=None)` | `16a_chap2_esg_scoring.ipynb` | Monte Carlo simulation of a power-law-weighted portfolio score. | A generic simulation utility parameterized by alpha grid, draw count, and asset count; reused three times. | None beyond `numpy.random` itself (already used internally) — the power-law weighting/normalization scheme is project-specific. | 🔵 |
| `compute_portfolio_frontier(...)` | `16b_chap3_esg_investing.ipynb` | Builds an efficient frontier (absolute, or benchmark-relative tracking-error) over a target grid, with bisection on the risk-aversion parameter to hit exact return/vol targets. | Fully generic frontier-construction engine — every input is a parameter — and the workhorse behind ~8 calls throughout `16b` via two thin wrappers. | Partial — the third-party `PyPortfolioOpt` package already builds efficient frontiers (including benchmark-relative/tracking-error variants) end to end; it isn't a dependency of this project yet, but if the project were open to adding one, it's the closest existing library match rather than a `scipy.optimize.minimize`-based build from scratch. | 🔵 |
| `fn_delta_t(...)` | `16c_chap5_biodiversity_dynamics.ipynb` | Net species-richness growth rate of a colonization-extinction equilibrium model, given fully general model parameters. | No hardcoded parameters; used for equilibrium root-finding, ODE trajectory integration, and several comparative-statics sweeps. | None — a specific ecological model, not a generic numerical primitive. | 🔵 |
| `pdf_poisson_binomial_dp(p)` | `16d_chap5_risk_and_health_economics.ipynb` | Exact PMF of a Poisson-binomial random variable via an O(n²) DP/convolution recursion. | Fully generic probability utility, cross-validated against an FFT-based method and the Binomial special case. | Partial — `scipy.stats` has no Poisson-binomial distribution built in; the small dedicated PyPI package `poibin` implements this exact PMF (via the DFT method), so a real option exists, just not in `numpy`/`scipy`/`pandas` themselves. | 🔵 |
| `pdf_poisson_binomial_dft(p)` | `16d_chap5_risk_and_health_economics.ipynb` | Same Poisson-binomial PMF via the discrete Fourier transform of its characteristic function. | An independent, equally generic algorithm for the same problem — useful as a cross-check or for larger n. | Same as `pdf_poisson_binomial_dp` above — `poibin` (PyPI) already implements this DFT approach directly; not in the standard stack itself. | 🔵 |
| `cobb_douglas_welfare(alpha, p1_t0, p1_t1, p2, y, label)` | `16e_chap10_transition_risk_welfare.ipynb` | Given Cobb-Douglas preference/price/income parameters for a before/after price shock, computes optimal demand, utility, and CV/EV welfare measures, both analytically and numerically. | Fully generic consumer-welfare calculation, reused for two separate exercises. | None beyond `scipy.optimize.minimize` (already used internally for the numerical cross-check) — the Cobb-Douglas CV/EV formulas themselves aren't packaged anywhere. | 🔵 |
